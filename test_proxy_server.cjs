const express = require('express');
const https = require('https');
const http = require('http');
const cors = require('cors'); // Need to install cors if not exists

// We will add an endpoint to our existing Express server
